@using TeknoramaUI.Areas.Administration.Models.ExpenseModel
@model List<ExpenseListResponseModel>
@{
    ViewData["Title"] = "List";
    Layout = "~/Areas/Administration/Views/Shared/_AdminLayout.cshtml";
}

<table class="table table-bordered" style="margin-top:20px">
    <tr>
        <th>Id</th>
        <th>Amount</th>
        <th>Description</th>
        <th>Expense Type</th>
        <th>Payment Date</th>
        <th>Update</th>
        <th>Delete</th>
    </tr>
    @if (Model.Count > 0)
    {
        @foreach (var item in Model)
        {
            <tr>
                <th>@item.Id</th>
                <th>@item.Amount</th>
                <th>@item.Description</th>
                <th>@item.ExpenseType</th>
                <th>@item.PaymentDate</th>
                <th colspan="1">
                    <a class="btn btn-outline-warning btn-sm" asp-route-id="@item.Id" asp-action="Update" asp-controller="Expense">Update</a>
                </th>
                <th colspan="2">
                    <a class="btn btn-outline-danger btn-sm" asp-route-id="@item.Id" asp-action="Delete" asp-controller="Expense">Delete</a>
                </th>
            </tr>
        }
    }
    else
    {
        <tr>
            <td colspan="2" class="text-danger">There are no data found</td>
        </tr>
    }
</table>
<button href="/Administration/Expense/Create" class="btn btn-primary">Add Expense</button>